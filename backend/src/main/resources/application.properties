
# Allow platform (Render) to supply the port via the `PORT` env var; fall back to 3039 locally
server.port=${PORT:3039}
# Read datasource configuration from environment when available; keep current values as defaults for local dev
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://mysql-1d0db3a8-cognizan-6176.a.aivencloud.com:11278/defaultdb?ssl-mode=REQUIRED}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:avnadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:AVNS_VnAkOtliLCkImw36RQ4}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true


app.jwt.secret=ChangeThisSecretKeyToAtLeast256BitsLongForProd
app.jwt.expiration-ms=86400000

app.interest.monthly-rate=2.0


